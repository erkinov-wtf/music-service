production_configs:
  server:
    port: 8083
    host: "localhost"

  database:
    host: "localhost"
    port: "5432"
    name: "postgres"
    user: "postgres"
    schema: "public"
    password: "postgres" # will be overwritten from os.Getenv()